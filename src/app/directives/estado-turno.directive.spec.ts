import { EstadoTurnoDirective } from './estado-turno.directive';

describe('EstadoTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
