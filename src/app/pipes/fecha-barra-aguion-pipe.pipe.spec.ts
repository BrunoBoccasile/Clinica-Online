import { FechaBarraAGuionPipePipe } from './fecha-barra-aguion-pipe.pipe';

describe('FechaBarraAGuionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaBarraAGuionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
