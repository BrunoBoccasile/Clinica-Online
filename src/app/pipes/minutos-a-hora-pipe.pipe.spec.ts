import { MinutosAHoraPipePipe } from './minutos-a-hora-pipe.pipe';

describe('MinutosAHoraPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinutosAHoraPipePipe();
    expect(pipe).toBeTruthy();
  });
});
