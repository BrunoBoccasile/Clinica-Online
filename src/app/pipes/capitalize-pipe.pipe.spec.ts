import { CapitalizePipePipe } from './capitalize-pipe.pipe';

describe('CapitalizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
