import { ArrayToStringPipePipe } from './array-to-string-pipe.pipe';

describe('ArrayToStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
